settings:
  logs: false # вкл логов в каталоге rosorter. logs.txt
  daemon: false # Включение демона(будет находится в трее)
  timeout: "60m" # время сортировки(0m сортировка ручная по требованию юзера)
  gui: false # Использование диалогов в tk
  silent: true # Работа в тихую без вывода уведов об сортировке

directories: # Указание всех директории
  downloads: # Название каталога
    path: "/home/user/Downloads" # Путь
    files: ["png", "jpg", "pdf", "directory"] # То что парсим
    names: # Указываем каталоги(обязательно)
    # - расширение:каталог
      - "png": "Photos"
      - "jpg": "Photos"
      - "pdf": "Docs"
      - "directory": "Folders"

  documents:
    path: "/home/user/Documents"
    files: ["*"] # Все парсим
    names:
      - "pdf": "Files"
      - "*": "*" # Все парсим по своим каталогам(conf = каталог будет conf)


# Код конфига необходимо указывать по порядку как было указанно
# команды
# rosorter run - запустить сортировку ручно
# rosorter ls settings/catalogs - вывод включенных настроек и каталогов
# rosorter ls catalog Documents - вывод настройки каталога и что отсортированно
# rosorter help - помощь
# rosorter generate_config - сгенерировать конфиг

# $ ~ rosorter run
# [rosorter] : Инициализация конфигурации
# [rosorter] : Конфиг отсуствует! Создаю конфигурацию
# [rosorter] : Выход
# -=-=-
# [rosorter] : Найдены 2 настройки(Downloads, Documents)
# [rosorter] : Сортировка Downloads
# [rosorter] : Сортировка Documents
# [rosorter] : Успешно! Отсортированно N файлов

# $ ~ rosorter ls settings
# [rosorter] : Настройки
# - Logs: False
# - Daemon: False
# - TimeOut: 60 minutes
# - GUI: False
# - Silent: True

# $ ~ rosorter ls documents
# [rosorter] : Настройки
# - Logs: False
# - Daemon: False
# - TimeOut: 60 minutes
# - GUI: False
# - Silent: True
